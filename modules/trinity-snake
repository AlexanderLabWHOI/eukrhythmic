configfile: "config.yaml"

OUTPUTDIR = config['outputDIR']

import io
from snakemake.exceptions import print_exception, WorkflowError

rule trinity:
	input:
		left = OUTPUTDIR + "/firsttrim/{sample}_1.trimmed.fastq.gz",
		right = OUTPUTDIR + "/firsttrim/{sample}_2.trimmed.fastq.gz"
	output:	
		OUTPUTDIR + "/trinity_results/Trinity_{sample}.fasta"
	log:
		"logs/trinity/{sample}_outputlog_trinity.log"
	params:
		extra = ""
	threads: 4
	wrapper:	
		"0.36.0/bio/trinity"


