configfile: "config.yaml"

OUTPUTDIR = config["outputDIR"]
SCRATCHDIR = config["scratch"]
INPUTDIR = config["inputDIR"]

import io
import os
import pathlib
from snakemake.exceptions import print_exception, WorkflowError

rule fastqc_trimmed:
	input:
		os.path.join(OUTPUTDIR, "firsttrim", "{sample}_{oldext}_1.trimmed.fastq.gz")
	output:
		html = os.path.join(OUTPUTDIR, "qc", "fastqc_trimmed", "{sample}_{oldext}.trimmed.html"),
		zip = os.path.join(OUTPUTDIR, "qc", "fastqc_trimmed", "{sample}_{oldext}.trimmed.zip")
	params: ""
	log: 
		"logs/fastqc/{sample}_{oldext}.trimmed.log"
	wrapper:
		"0.36.0/bio/fastqc"
